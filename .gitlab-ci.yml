# image: ruby:2.1   #默认的镜像
image: bash:latest

pages:
  # stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - rm -rf public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
